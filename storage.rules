rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    match /{allPaths=**} {

      // Allow public reading of portfolio images
      allow read: if true;

      // Only logged-in users can upload images
      allow write: if request.auth != null;

    }

  }
}